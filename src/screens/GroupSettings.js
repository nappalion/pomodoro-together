/*
Displays the settings for a group.
User can change the group name, accessibility, etc.
They can also view the users and remove them (add second prompt to confirm choice).
Search bar at the top to search for users.
Ability to delete group (add second prompt to confirm choice).

*/

import React from 'react';

function GroupSettings(props) {

    return(
        <span style={{color: 'black'}}>group settings screen</span>
    );
}

export default GroupSettings;