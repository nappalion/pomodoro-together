/*
Page for adding users to a group.
Navigate here from the Group Settings Screen (add user icon)
Holds a search bar and a list that displays the results for the user's search

*/

import React from 'react';

function GroupAddUser(props) {

    return(
        <text style={{color: 'black'}}>add group screen</text>
    );
}

export default GroupAddUser;